(library
 ((name shexp_bigstring)
  (public_name shexp.bigstring)
  (c_names (bigstring_stubs))
  ;; No deps outside of the compiler distribution!
  (libraries (bigarray))
  (preprocess no_preprocessing)))


(jbuild_version 1)
