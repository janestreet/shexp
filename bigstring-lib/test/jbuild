(library
 ((name shexp_bigstring_test)
(flags (:standard -safe-string))
  (libraries (shexp_bigstring core expect_test_helpers_kernel))
  (preprocess (pps (ppx_jane ppxlib.runner)))))


(jbuild_version 1)
