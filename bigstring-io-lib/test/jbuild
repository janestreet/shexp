(library
 ((name shexp_bigstring_io_test)
(flags (:standard -safe-string))
  (libraries (shexp_bigstring_io core expect_test_helpers_kernel unix))
  (preprocess (pps (ppx_jane ppx_driver.runner)))))

(jbuild_version 1)
